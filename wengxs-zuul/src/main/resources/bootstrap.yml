server:
  port: 8080

spring:
  application:
    name: wengxs-zuul
  cloud:
    config:
      fail-fast: true
      name: ${spring.application.name}
#      profile:
      discovery:
        enabled: true
        service-id: wengxs-config
    nacos
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
