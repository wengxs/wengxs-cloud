server:
  port: 8012
spring:
  application:
    name: wengxs-consumer
#  profiles:
#    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
    sentinel:
      transport:
        dashboard: localhost:8888
feign:
  sentinel:
    enabled: true
