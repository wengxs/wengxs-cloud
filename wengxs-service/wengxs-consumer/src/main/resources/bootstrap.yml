server:
  port: 8012
spring:
  application:
    name: wengxs-consumer
#  profiles:
#    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
feign:
  hystrix:
    enabled: true
